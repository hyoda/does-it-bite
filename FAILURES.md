# 확인 가능성이 어긋나는 여섯 자리

> 여기 적힌 것은 가정이 아니다. 전부 **한 저장소에서 실제로 일어난 일**이고,
> 대부분 내가 낸 것이다. 날짜를 붙인 것은 자책이 아니라 **재현 가능성** 때문이다.

버그는 "코드가 틀렸다"에서 끝나지 않는다. 더 비싼 것은 **틀렸다는 걸 알 수 없는 상태**다.
그 상태는 여섯 가지 모양으로 온다. 각각 고치는 방법이 다르다.

---

## F1 · 게이트가 없었다

가장 흔하고 가장 눅눅한 실패. 규칙은 있는데 그 규칙을 지키는지 보는 것이 없다.

**실제 사례 (2026-08-21).** 사이트에 페이지를 하나 만들어 배포했다. 잘 썼다.
`sitemap.xml` 에도 실렸다. 그런데 **어느 페이지에서도 링크하지 않았다.**
인바운드 0. 기계는 사이트맵으로 찾았고 사람은 못 찾았다.
3주 동안 아무도 그 페이지를 열지 않았다. 사이트의 주제가 "발견되게 만드는 일"이었다.

**왜 안 걸렸나.** "새 페이지를 만들면 링크한다"는 규칙은 있었다. 머릿속에.
머릿속의 규칙은 바쁠 때 가장 먼저 증발한다.

**고침.** [`checks/orphan_pages.py`](checks/orphan_pages.py) — 인바운드 0인 페이지를 찾는다.
그리고 링크 목록을 **손으로 들지 않는다.** 손으로 든 목록은 다음 페이지에서 또 빠진다.
배포된 경로에서 세게 했다.

> **습관은 검사가 아니다.** 같은 실수를 세 번 하고 나서야 검사로 바꾼 항목이 여럿 있다.
> 두 번째에서 바꿨어야 했다.

---

## F2 · 검사기가 아무것도 안 보고 있었다

검사가 있다. 초록불이 뜬다. **그런데 아무것도 안 본다.**
이건 게이트가 없는 것보다 나쁘다 — 없으면 불안하기라도 한데, 이건 안심시킨다.

**실제 사례 (2026-08-21).** "각주 번호 중복" 검사가 몇 주째 통과하고 있었다.
변이 시험을 돌려보니 **번호를 실제로 중복시켜도 통과했다.**
정규식이 원래 매치되지 않는 형태를 찾고 있었고, 매치가 0건이면 중복도 0건이라 통과였다.

**왜 안 걸렸나.** 검사를 쓴 뒤 **실패시켜 본 적이 없다.**
통과하는 걸 보고 "됐다"고 넘어갔다. 통과는 두 가지를 뜻한다 —
문제가 없거나, **검사가 문제를 못 보거나.** 통과만으로는 둘을 구별할 수 없다.

**고침.** [`mutation/mutate.sh`](mutation/mutate.sh) — 틀린 것을 넣고 무는지 본다.
새 검사를 쓰면 **반드시 한 번 실패시켜 본다.** 실패를 못 본 검사는 검사가 아니다.

---

## F3 · 틀린 것을 쟀다

값이 틀린 게 아니다. **재는 대상이 틀렸다.** 측정 도구는 정직하게 답했다.

**사례 A — 사람에게 보여주려고 만든 문자열을 사실로 읽었다 (2026-08-16 → 08-21 발견).**
영상 페이지에서 발표 날짜를 읽었다:

```
"dateText":       "실시간 스트리밍 시작일: 2026. 6. 23."   ← 사람에게 보여주는 문자열
"startTimestamp": "2026-06-24T04:58:42+00:00"             ← 기계 타임스탬프
```

표시 문자열을 읽었고, **그 문자열은 페이지를 받아온 쪽의 시간대로 렌더된다.**
크롤러가 미국에 있어 하루가 밀렸다. 5일 뒤 지역 신문 기사가 "지난 24일"이라고 써서 어긋남이 드러났다.
같은 페이지에 시간대가 붙은 기계 값이 있었다.

**사례 B — 도구가 잰 것이 내가 재려던 것이 아니었다.**
페이지 본문 길이를 재려고 정규식으로 `<script>` 를 지웠다.
대상이 한 줄짜리 HTML 이라 탐욕적 매칭이 **문서 전체를 지웠다.**
"본문 1 byte"라고 보고했다. 다시 재니 4,618 자였다.

**사례 C — 시험이 파이프라인에 덮였다 (2026-08-21).**
검사기가 무는지 보려고 **생성물**을 변이시켰다. 검사는 통과를 보고했다.
검사의 첫 단계가 생성물을 다시 짓느라 변이를 덮어썼기 때문이다.
검사는 변이를 본 적이 없다. **생성 파이프라인 앞에서 생성물을 변이시키는 시험은 무효다.**

**고침.** 셋 다 같은 처방이다 — **잰 것이 재려던 것인지 한 번 되묻는다.**
사람용 표현이 아니라 기계 값을 읽고, 도구가 지운 것을 확인하고, 정본을 변이시킨다.
`mutate.sh` 는 사례 C 를 자동으로 잡는다: 검사 실행 뒤 파일이 그대로인지 보고,
아니면 `무효` 로 판정한다. 통과/실패 어느 쪽으로도 세지 않는다.

---

## F4 · 같은 사실을 두 곳에서 짓고 있었다

생성기가 둘이면 규칙도 둘이 된다. 한쪽만 고쳐도 아무도 알려주지 않는다.

**실제 사례 (2026-08-20, 08-21 재발).** 같은 구조화 데이터를 두 생성기가 지었다 —
하나는 API 파일, 하나는 페이지 `<head>`. 규칙이 갈려 있었고,
한쪽이 **관계없는 사이트를 "같은 엔터티"로 선언**하고 있었다.

고친 다음날 같은 자리에서 또 났다. 새 필드를 한쪽에만 넣었고,
그쪽만 확인하고 "반영됐다"고 결론지었다. 실제 페이지에는 안 실렸다.

**왜 안 걸렸나.** 두 생성기의 존재는 알고 있었다.
**둘이 같은 것을 내는지 보는 검사가 없었다.**

**고침.** [`checks/generator_parity.py`](checks/generator_parity.py) —
지정한 경로를 여러 산출물에서 뽑아 대조한다.
그리고 두 생성기 코드에 **서로를 가리키는 주석**을 남겼다.
"여기를 고치면 저기도 고친다"는 다음 사람에게 보이는 자리에 있어야 한다.

---

## F5 · 표면이 거짓 검증가능성을 줬다

근거를 확인했다. 그 근거가 근거가 아니었다.

**사례 A — 파는 페이지를 제품의 증거로 읽었다.**
"이 기능은 이미 있다"고 문서에 썼다. 근거는 그 기능을 소개하는 페이지였다.
소스를 열어보니 **701줄짜리 랜딩 페이지**였다. 로그인도 API 도 없었다.
**파는 페이지는 제품의 증거가 아니다.**

**사례 B — 남의 "확인했다"를 근거로 커밋했다.**
"영향 없음 확인"이라는 보고를 받아 문서에 실었다.
그 확인은 출력을 필터로 걸러 보고 있었고, 필터가 문제를 가리고 있었다.
직접 돌려보니 문제가 있었다.

**사례 C — 순서가 뒤집혔다.**
A 와 B 가 서로를 가리켜야 성립하는 관계에서, **A 를 B 등록 전에 배포**하고
"결속 성립"이라고 쓸 뻔했다. A 는 아직 존재하지 않는 B 를 가리키고 있었다.

**고침.** 근거를 인용하기 전에 **그 근거가 무엇을 보고 있는지** 한 겹 더 본다.
"확인했다"는 결론이지 근거가 아니다. 근거는 **무엇을 어떻게 봤는가**다.

---

## F6 · 거울상 오경보

검사가 물었다. 그런데 문제는 반대쪽에 있었다.

**실제 사례.** "이 파일에 값이 없다"고 검사가 실패했다.
값을 넣었다. 다음 실행에서 다른 검사가 실패했다 — **넣은 값이 틀린 자리에 있었다.**
첫 검사가 맞았지만 그것이 가리킨 방향이 틀렸다.

**고침.** 검사가 실패하면 **먼저 무엇을 근거로 실패했는지 읽는다.**
실패 메시지를 사라지게 하는 최단 경로로 가면, 검사를 만족시키고 문제는 남는다.
그래서 이 저장소의 검사기들은 **판정과 함께 근거를 낸다** — 무엇을 봤고 무엇을 기대했는지.

---

## 요약 — 새 검사를 쓸 때 묻는 다섯

1. **이 검사를 실패시켜 봤는가?** (F2) — 안 해봤으면 검사가 아니다
2. **내가 잰 것이 재려던 것인가?** (F3) — 사람용 표현 말고 기계 값
3. **같은 사실을 짓는 곳이 또 있는가?** (F4) — 있으면 둘을 대조하는 검사가 따로 필요하다
4. **이 근거는 무엇을 보고 있는가?** (F5) — "확인했다"는 근거가 아니다
5. **목록을 손으로 들고 있는가?** (F1) — 손으로 든 목록은 다음번에 빠진다
